# Minimal Example

The runnable minimal application lives in [`../../orbit-example`](../../orbit-example/).

From the repository root:

```sh
moon update
moon run orbit-example
```

That package contains the schema-version 2 configuration, embedded frontend,
generated MoonBit source, IPC bindings, and native application entry point.
