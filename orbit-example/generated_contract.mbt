// Generated by orbit-build. DO NOT EDIT.

///|
pub(all) suberror OrbitContractError { OrbitContractError(String) }

///|
fn[T] orbit_contract_fail(path : String) -> T raise OrbitContractError { raise OrbitContractError("invalid value at " + path) }
///|
pub(all) enum OrbitStatus {
  Idle
  Running
  Failed
} derive(Eq, Debug)

///|
pub fn orbit_contract_decode_0(value : Json, path : String) -> OrbitStatus raise OrbitContractError {
  match value {
    "idle" => OrbitStatus::Idle
    "running" => OrbitStatus::Running
    "failed" => OrbitStatus::Failed
    _ => orbit_contract_fail(path)
  }
}

///|
pub fn orbit_contract_encode_0(value : OrbitStatus, path : String) -> Json {
  ignore(path)
  match value {
    OrbitStatus::Idle => Json::string("idle")
    OrbitStatus::Running => Json::string("running")
    OrbitStatus::Failed => Json::string("failed")
  }
}

///|
pub(all) enum OrbitContract3 {
  Failed
} derive(Eq, Debug)

///|
pub fn orbit_contract_decode_3(value : Json, path : String) -> OrbitContract3 raise OrbitContractError {
  match value {
    "failed" => OrbitContract3::Failed
    _ => orbit_contract_fail(path)
  }
}

///|
pub fn orbit_contract_encode_3(value : OrbitContract3, path : String) -> Json {
  ignore(path)
  match value {
    OrbitContract3::Failed => Json::string("failed")
  }
}

///|
pub fn orbit_contract_decode_4(value : Json, path : String) -> String raise OrbitContractError {
  match value { String(text) => text; _ => orbit_contract_fail(path) }
}

///|
pub fn orbit_contract_encode_4(value : String, path : String) -> Json {
  ignore(path)
  Json::string(value)
}

///|
pub(all) struct OrbitOperationFailed {
  kind : OrbitContract3
  message : String
} derive(Debug)

///|
pub fn orbit_contract_decode_2(value : Json, path : String) -> OrbitOperationFailed raise OrbitContractError {
  guard value is Object(items) else { orbit_contract_fail(path) }
  for key, _ in items { if !["kind","message"].contains(key) { orbit_contract_fail(path + "." + key) } }
  {
    kind: orbit_contract_decode_3(match items.get("kind") { Some(item) => item; None => orbit_contract_fail(path + "." + "kind") }, path + "." + "kind"),
    message: orbit_contract_decode_4(match items.get("message") { Some(item) => item; None => orbit_contract_fail(path + "." + "message") }, path + "." + "message"),
  }
}

///|
pub fn orbit_contract_encode_2(value : OrbitOperationFailed, path : String) -> Json {
  let result : Map[String, Json] = Map([])
  result["kind"] = orbit_contract_encode_3(value.kind, path + "." + "kind")
  result["message"] = orbit_contract_encode_4(value.message, path + "." + "message")
  Json::object(result)
}

///|
pub fn orbit_contract_decode_6(value : Json, path : String) -> String raise OrbitContractError {
  match value { String(text) => text; _ => orbit_contract_fail(path) }
}

///|
pub fn orbit_contract_encode_6(value : String, path : String) -> Json {
  ignore(path)
  Json::string(value)
}

///|
pub(all) enum OrbitContract7 {
  Started
} derive(Eq, Debug)

///|
pub fn orbit_contract_decode_7(value : Json, path : String) -> OrbitContract7 raise OrbitContractError {
  match value {
    "started" => OrbitContract7::Started
    _ => orbit_contract_fail(path)
  }
}

///|
pub fn orbit_contract_encode_7(value : OrbitContract7, path : String) -> Json {
  ignore(path)
  match value {
    OrbitContract7::Started => Json::string("started")
  }
}

///|
pub(all) struct OrbitOperationStarted {
  id : String
  kind : OrbitContract7
} derive(Debug)

///|
pub fn orbit_contract_decode_5(value : Json, path : String) -> OrbitOperationStarted raise OrbitContractError {
  guard value is Object(items) else { orbit_contract_fail(path) }
  for key, _ in items { if !["id","kind"].contains(key) { orbit_contract_fail(path + "." + key) } }
  {
    id: orbit_contract_decode_6(match items.get("id") { Some(item) => item; None => orbit_contract_fail(path + "." + "id") }, path + "." + "id"),
    kind: orbit_contract_decode_7(match items.get("kind") { Some(item) => item; None => orbit_contract_fail(path + "." + "kind") }, path + "." + "kind"),
  }
}

///|
pub fn orbit_contract_encode_5(value : OrbitOperationStarted, path : String) -> Json {
  let result : Map[String, Json] = Map([])
  result["id"] = orbit_contract_encode_6(value.id, path + "." + "id")
  result["kind"] = orbit_contract_encode_7(value.kind, path + "." + "kind")
  Json::object(result)
}

///|
pub(all) enum OrbitOperation {
  Failed(OrbitOperationFailed)
  Started(OrbitOperationStarted)
} derive(Debug)

///|
pub fn orbit_contract_decode_1(value : Json, path : String) -> OrbitOperation raise OrbitContractError {
  guard value is Object(items) else { orbit_contract_fail(path) }
  let tag_value = match items.get("kind") { Some(String(value)) => value; _ => orbit_contract_fail(path + "." + "kind") }
  match tag_value {
    "failed" => OrbitOperation::Failed(orbit_contract_decode_2(value, path))
    "started" => OrbitOperation::Started(orbit_contract_decode_5(value, path))
    _ => orbit_contract_fail(path + "." + "kind")
  }
}

///|
pub fn orbit_contract_encode_1(value : OrbitOperation, path : String) -> Json {
  ignore(path)
  match value {
    OrbitOperation::Failed(payload) => orbit_contract_encode_2(payload, path)
    OrbitOperation::Started(payload) => orbit_contract_encode_5(payload, path)
  }
}

///|
pub fn orbit_contract_decode_9(value : Json, path : String) -> String raise OrbitContractError {
  match value { String(text) => text; _ => orbit_contract_fail(path) }
}

///|
pub fn orbit_contract_encode_9(value : String, path : String) -> Json {
  ignore(path)
  Json::string(value)
}

///|
pub(all) struct OrbitPingResponse {
  message : String
} derive(Debug)

///|
pub fn orbit_contract_decode_8(value : Json, path : String) -> OrbitPingResponse raise OrbitContractError {
  guard value is Object(items) else { orbit_contract_fail(path) }
  for key, _ in items { if !["message"].contains(key) { orbit_contract_fail(path + "." + key) } }
  {
    message: orbit_contract_decode_9(match items.get("message") { Some(item) => item; None => orbit_contract_fail(path + "." + "message") }, path + "." + "message"),
  }
}

///|
pub fn orbit_contract_encode_8(value : OrbitPingResponse, path : String) -> Json {
  let result : Map[String, Json] = Map([])
  result["message"] = orbit_contract_encode_9(value.message, path + "." + "message")
  Json::object(result)
}

///|
pub fn orbit_contract_decode_10(value : Json, path : String) -> Unit raise OrbitContractError {
  match value { Null => (); _ => orbit_contract_fail(path) }
}

///|
pub fn orbit_contract_encode_10(value : Unit, path : String) -> Json {
  ignore(value)
  ignore(path)
  Json::null()
}

///|
pub fn orbit_contract_decode_12(value : Json, path : String) -> Int raise OrbitContractError {
  match value { Number(number, ..) if number >= -2147483648.0 && number <= 2147483647.0 && number.to_int().to_double() == number => number.to_int(); _ => orbit_contract_fail(path) }
}

///|
pub fn orbit_contract_encode_12(value : Int, path : String) -> Json {
  ignore(path)
  Json(value)
}

///|
pub(all) struct OrbitContract11 {
  revision : Int
} derive(Debug)

///|
pub fn orbit_contract_decode_11(value : Json, path : String) -> OrbitContract11 raise OrbitContractError {
  guard value is Object(items) else { orbit_contract_fail(path) }
  for key, _ in items { if !["revision"].contains(key) { orbit_contract_fail(path + "." + key) } }
  {
    revision: orbit_contract_decode_12(match items.get("revision") { Some(item) => item; None => orbit_contract_fail(path + "." + "revision") }, path + "." + "revision"),
  }
}

///|
pub fn orbit_contract_encode_11(value : OrbitContract11, path : String) -> Json {
  let result : Map[String, Json] = Map([])
  result["revision"] = orbit_contract_encode_12(value.revision, path + "." + "revision")
  Json::object(result)
}

///|
pub fn register_example_ping(registry : @ipc.CommandRegistry, handler : (Unit) -> Result[OrbitPingResponse, @ipc.CommandFailure]) -> Unit raise @ipc.IpcError {
  register_example_ping_context(registry, fn(_, request) { handler(request) })
}

///|
pub fn register_example_ping_context(registry : @ipc.CommandRegistry, handler : (@ipc.InvocationContext, Unit) -> Result[OrbitPingResponse, @ipc.CommandFailure]) -> Unit raise @ipc.IpcError {
  registry.register_context_json(@ipc.CommandName::new("example.ping"), fn(context, payload) {
    let request = orbit_contract_decode_10(payload, "request") catch { OrbitContractError(message) => return Err(@ipc.CommandFailure::new(code="invalid_payload", message=message)) }
    match handler(context, request) {
      Err(error) => Err(error)
      Ok(response) => {
        let result = orbit_contract_encode_8(response, "response")
        Ok(result)
      }
    }
  })
}

///|
pub fn register_async_example_ping(registry : @ipc.CommandRegistry, handler : async (Unit) -> Result[OrbitPingResponse, @ipc.CommandFailure]) -> Unit raise @ipc.IpcError {
  register_async_example_ping_context(registry, async fn(_, request) { handler(request) })
}

///|
pub fn register_async_example_ping_context(registry : @ipc.CommandRegistry, handler : async (@ipc.InvocationContext, Unit) -> Result[OrbitPingResponse, @ipc.CommandFailure]) -> Unit raise @ipc.IpcError {
  registry.register_async_context_json(@ipc.CommandName::new("example.ping"), async fn(context, payload) {
    let request = orbit_contract_decode_10(payload, "request") catch { OrbitContractError(message) => return Err(@ipc.CommandFailure::new(code="invalid_payload", message=message)) }
    match handler(context, request) {
      Err(error) => Err(error)
      Ok(response) => {
        let result = orbit_contract_encode_8(response, "response")
        Ok(result)
      }
    }
  })
}

///|
pub fn event_app_changed(payload : OrbitContract11, target? : @event.EventTarget = @event.Any, source? : @event.EventTarget = @event.Application) -> @event.EventEnvelope {
  let payload = orbit_contract_encode_11(payload, "event")
  let name = @event.EventName::new("app.changed") catch { _ => abort("generated Orbit event name is invalid") }
  @event.EventEnvelope::new(name~, payload~, target~, source~)
}

///|
pub fn event_app_operation(payload : OrbitOperation, target? : @event.EventTarget = @event.Any, source? : @event.EventTarget = @event.Application) -> @event.EventEnvelope {
  let payload = orbit_contract_encode_1(payload, "event")
  let name = @event.EventName::new("app.operation") catch { _ => abort("generated Orbit event name is invalid") }
  @event.EventEnvelope::new(name~, payload~, target~, source~)
}
